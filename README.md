# WES-Img-Prep-for-LoRA
为 LoRA 训练而生的图像批量处理工具（GUI+CLI，中英双语，断点续传，WebP 转换，智能命名）A bilingual batch image processor built for LoRA training (GUI + CLI, resumable processing, WebP conversion, smart renaming)
